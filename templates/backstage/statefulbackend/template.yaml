apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: statefulbackend
  title: "Statefulbackend"
  description: >-
    A backend and the database it owns, as one bundle — a Prometeo StatefulBackend. Use this when
    a backend owns its database exclusively; when several services share one database, use the
    "service" template plus a separate "database" instead. The product must already exist (run
    the "product" template first).
  tags:
    - prometeo
spec:
  owner: guests
  type: service
  parameters:
    - title: Placement
      required:
        - name
        - product
        - image
      properties:
        name:
          title: Service name
          type: string
          description: >-
            e.g. "add-be". Becomes the XR name and its own namespace, shared with the database it
            owns.
          pattern: '^[a-z][a-z0-9-]*$'
          ui:autofocus: true
        product:
          title: Product
          type: string
          description: The product this backend belongs to (must already exist in prometeo-products).
          ui:field: EntityPicker
          ui:options:
            catalogFilter:
              kind: System
            defaultKind: System
        image:
          title: Container image
          type: string
        containerPort:
          title: Container port
          type: integer
          default: 8000
        replicas:
          title: Replicas
          type: integer
    - title: Runtime
      properties:
        env:
          title: Environment variables
          type: object
          additionalProperties:
            type: string
          default: {}
        migrationsEnabled:
          title: Run schema migrations before rollout
          type: boolean
          default: false
    - title: AWS access
      properties:
        awsBedrock:
          title: Bedrock
          type: boolean
          default: false
        awsS3:
          title: S3
          type: boolean
          default: false
        awsSqs:
          title: SQS
          type: boolean
          default: false
        awsDynamodb:
          title: DynamoDB
          type: boolean
          default: false
    - title: Network
      properties:
        allowFrom:
          title: Allow from
          type: array
          items:
            type: object
            required: [service, port]
            properties:
              service:
                type: string
                ui:field: EntityPicker
                ui:options:
                  catalogFilter:
                    kind: Component
                  defaultKind: Component
              port:
                type: integer
          default: []
        allowTo:
          title: Allow to
          type: array
          items:
            type: object
            required: [service, port]
            properties:
              service:
                type: string
                ui:field: EntityPicker
                ui:options:
                  catalogFilter:
                    kind: Component
                  defaultKind: Component
              port:
                type: integer
          default: []
    - title: Its database
      properties:
        databaseName:
          title: Database name inside the cluster
          type: string
          description: Defaults to "<product>db" (hyphens removed) when left blank.
        engineVersion:
          title: Aurora PostgreSQL version
          type: string
        minACU:
          title: Min ACU
          type: number
        maxACU:
          title: Max ACU
          type: number
        instances:
          title: Instances
          type: integer
          default: 1
        schemas:
          title: Schemas to create at bootstrap
          type: array
          items:
            type: string
          default: []
        deletionProtection:
          title: Deletion protection
          type: boolean
          default: false

  steps:
    # -----------------------------------------------------------------------------------
    # Connectivity is two-ended, so the peer's manifest has to change too.
    #
    # A NetworkPolicy can only select pods in its own namespace, and every service IS its own
    # namespace. So an `allowFrom` entry naming tief-fe renders this service's ingress rule and
    # nothing else — tief-fe's egress rule can only be written into tief-fe's namespace, by
    # tief-fe's own XR. On top of that, `allowTo` is what emits <PEER>_HOST into the caller's
    # base envs (app-service composition, "One <PEER>_HOST per declared outbound dependency"),
    # so the caller needs the entry for service discovery even where egress is already open.
    #
    # Both halves therefore live in the peer's file. These steps fetch the existing manifests
    # and mirror each rule into its peer, so one form fills in opens the connection end to end
    # and the PR shows both sides.
    # -----------------------------------------------------------------------------------
    - id: fetch-products
      name: Fetch the existing manifests so peers can be patched
      action: fetch:plain
      input:
        # Only this product: connectivity is scoped to the product, so a peer is always a
        # sibling in the same directory.
        url: https://github.com/crossplane-poc/prometeo-products/tree/main/products/${{ parameters.product | parseEntityRef | pick('name') }}
        targetPath: products/${{ parameters.product | parseEntityRef | pick('name') }}

    - id: write-component
      name: Write the StatefulBackend manifest
      action: roadiehq:utils:fs:write
      input:
        path: products/${{ parameters.product | parseEntityRef | pick('name') }}/base/${{ parameters.name }}.yaml
        content: |
          apiVersion: v1
          kind: Namespace
          metadata:
            name: ${{ parameters.name }}
            labels:
              name: ${{ parameters.name }}
              service: ${{ parameters.name }}
              product: ${{ parameters.product | parseEntityRef | pick('name') }}
              platform: prometeo
              admission.datadoghq.com/enabled: "true"
          ---
          apiVersion: platform.prometeo.iag.ai/v1alpha1
          kind: StatefulBackend
          metadata:
            name: ${{ parameters.name }}
            namespace: ${{ parameters.name }}
          spec:
            product: ${{ parameters.product | parseEntityRef | pick('name') }}
            image: ${{ parameters.image | dump }}
            containerPort: ${{ parameters.containerPort }}
          {% if parameters.replicas %}
            replicas: {{ parameters.replicas }}
          {% endif %}
          {% if parameters.env and parameters.env | length %}
            env:
          {% for k, v in parameters.env %}
              {{ k }}: {{ v | dump }}
          {% endfor %}
          {% endif %}
            migrations:
              enabled: ${{ parameters.migrationsEnabled }}
            aws:
              bedrock: ${{ parameters.awsBedrock }}
              s3: ${{ parameters.awsS3 }}
              sqs: ${{ parameters.awsSqs }}
              dynamodb: ${{ parameters.awsDynamodb }}
          {% if (parameters.allowFrom and parameters.allowFrom | length) or (parameters.allowTo and parameters.allowTo | length) %}
            network:
          {% if parameters.allowFrom and parameters.allowFrom | length %}
              allowFrom:
          {% for r in parameters.allowFrom %}
                - service: {{ r.service | parseEntityRef | pick('name') }}
                  port: {{ r.port }}
          {% endfor %}
          {% endif %}
          {% if parameters.allowTo and parameters.allowTo | length %}
              allowTo:
          {% for r in parameters.allowTo %}
                - service: {{ r.service | parseEntityRef | pick('name') }}
                  port: {{ r.port }}
          {% endfor %}
          {% endif %}
          {% endif %}
            database:
          {% if parameters.databaseName %}
              databaseName: {{ parameters.databaseName | dump }}
          {% endif %}
          {% if parameters.engineVersion %}
              engineVersion: {{ parameters.engineVersion | dump }}
          {% endif %}
          {% if parameters.schemas and parameters.schemas | length %}
              schemas:
          {% for s in parameters.schemas %}
                - {{ s }}
          {% endfor %}
          {% endif %}
              capacity:
                instances: ${{ parameters.instances }}
          {% if parameters.minACU %}
                minACU: {{ parameters.minACU }}
          {% endif %}
          {% if parameters.maxACU %}
                maxACU: {{ parameters.maxACU }}
          {% endif %}
              deletionProtection: ${{ parameters.deletionProtection }}

    - id: write-catalog-info
      name: Write the Backstage Component entity
      action: roadiehq:utils:fs:write
      input:
        path: products/${{ parameters.product | parseEntityRef | pick('name') }}/components/${{ parameters.name }}.yaml
        content: |
          apiVersion: backstage.io/v1alpha1
          kind: Component
          metadata:
            name: ${{ parameters.name }}
            description: ${{ ('StatefulBackend ' + parameters.name + ' (product ' + (parameters.product | parseEntityRef | pick('name')) + ')') | dump }}
            annotations:
              backstage.io/kubernetes-label-selector: 'service=${{ parameters.name }}'
          spec:
            type: service
            lifecycle: experimental
            owner: guests
            system: ${{ parameters.product | parseEntityRef | pick('name') }}

    - id: mirror-allow-from
      name: Mirror each "allow from" rule as the peer's allowTo
      each: ${{ parameters.allowFrom }}
      action: roadiehq:utils:fs:replace
      input:
        files:
          # Four mutually exclusive anchors, applied in order: whichever shape the peer's
          # file is in, exactly one of them can match. Each is a pure insertion, so the
          # peer keeps its comments. The path is pinned to this product: a peer outside it
          # is not a legal target. No match (a typo, or a peer not in this product) is a
          # silent no-op, visible as a missing side in the PR diff.
          # 1. The key is already a block list -- insert at its head.
          - file: products/${{ parameters.product | parseEntityRef | pick('name') }}/base/${{ each.value.service | parseEntityRef | pick('name') }}.yaml
            matchRegex: true
            find: '\n    allowTo:\n'
            replaceWith: "\n    allowTo:\n      - { service: ${{ parameters.name }}, port: ${{ each.value.port }} }\n"
          # 2. The key is there but empty (`allowTo: []`, the XRD default) -- open a block list.
          - file: products/${{ parameters.product | parseEntityRef | pick('name') }}/base/${{ each.value.service | parseEntityRef | pick('name') }}.yaml
            matchRegex: true
            find: '\n    allowTo: \[\]\n'
            replaceWith: "\n    allowTo:\n      - { service: ${{ parameters.name }}, port: ${{ each.value.port }} }\n"
          # 3. A network: block with no allowTo key at all -- add the key. The anchor stops
          #    at `network:` without taking the newline: the lookahead needs that newline
          #    left in place to recognise a key sitting on the very next line.
          - file: products/${{ parameters.product | parseEntityRef | pick('name') }}/base/${{ each.value.service | parseEntityRef | pick('name') }}.yaml
            matchRegex: true
            find: '\n  network:(?![\s\S]*\n    allowTo:)'
            replaceWith: "\n  network:\n    allowTo:\n      - { service: ${{ parameters.name }}, port: ${{ each.value.port }} }"
          # 4. No network: block at all (lookahead) -- open one directly under spec:.
          - file: products/${{ parameters.product | parseEntityRef | pick('name') }}/base/${{ each.value.service | parseEntityRef | pick('name') }}.yaml
            matchRegex: true
            find: '\nspec:(?![\s\S]*\n  network:)'
            replaceWith: "\nspec:\n  network:\n    allowTo:\n      - { service: ${{ parameters.name }}, port: ${{ each.value.port }} }"

    - id: mirror-allow-to
      name: Mirror each "allow to" rule as the peer's allowFrom
      each: ${{ parameters.allowTo }}
      action: roadiehq:utils:fs:replace
      input:
        files:
          # Four mutually exclusive anchors, applied in order: whichever shape the peer's
          # file is in, exactly one of them can match. Each is a pure insertion, so the
          # peer keeps its comments. The path is pinned to this product: a peer outside it
          # is not a legal target. No match (a typo, or a peer not in this product) is a
          # silent no-op, visible as a missing side in the PR diff.
          # 1. The key is already a block list -- insert at its head.
          - file: products/${{ parameters.product | parseEntityRef | pick('name') }}/base/${{ each.value.service | parseEntityRef | pick('name') }}.yaml
            matchRegex: true
            find: '\n    allowFrom:\n'
            replaceWith: "\n    allowFrom:\n      - { service: ${{ parameters.name }}, port: ${{ each.value.port }} }\n"
          # 2. The key is there but empty (`allowFrom: []`, the XRD default) -- open a block list.
          - file: products/${{ parameters.product | parseEntityRef | pick('name') }}/base/${{ each.value.service | parseEntityRef | pick('name') }}.yaml
            matchRegex: true
            find: '\n    allowFrom: \[\]\n'
            replaceWith: "\n    allowFrom:\n      - { service: ${{ parameters.name }}, port: ${{ each.value.port }} }\n"
          # 3. A network: block with no allowFrom key at all -- add the key. The anchor stops
          #    at `network:` without taking the newline: the lookahead needs that newline
          #    left in place to recognise a key sitting on the very next line.
          - file: products/${{ parameters.product | parseEntityRef | pick('name') }}/base/${{ each.value.service | parseEntityRef | pick('name') }}.yaml
            matchRegex: true
            find: '\n  network:(?![\s\S]*\n    allowFrom:)'
            replaceWith: "\n  network:\n    allowFrom:\n      - { service: ${{ parameters.name }}, port: ${{ each.value.port }} }"
          # 4. No network: block at all (lookahead) -- open one directly under spec:.
          - file: products/${{ parameters.product | parseEntityRef | pick('name') }}/base/${{ each.value.service | parseEntityRef | pick('name') }}.yaml
            matchRegex: true
            find: '\nspec:(?![\s\S]*\n  network:)'
            replaceWith: "\nspec:\n  network:\n    allowFrom:\n      - { service: ${{ parameters.name }}, port: ${{ each.value.port }} }"
    - id: publish
      name: Open a PR with the new statefulbackend
      action: publish:github:pull-request
      input:
        repoUrl: github.com?repo=prometeo-products&owner=crossplane-poc
        branchName: scaffolder/statefulbackend-${{ parameters.name }}
        title: 'prometeo statefulbackend: ${{ parameters.name }}'
        description: >-
          Adds `products/${{ parameters.product | parseEntityRef | pick('name') }}/base/${{ parameters.name }}.yaml` (Namespace +
          StatefulBackend) and its Backstage Component entity, via the "statefulbackend" template.


          Also edits each peer named in "allow from" / "allow to": connectivity needs a rule at
          both ends, and the peer's rule can only be written in the peer's own manifest. Check
          both sides of the diff — a peer outside this product is skipped silently.

  output:
    links:
      - title: Open PR
        url: ${{ steps['publish'].output.remoteUrl }}
