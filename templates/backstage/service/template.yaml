apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: service
  title: "Service"
  description: >-
    One runnable service (a Deployment, its AWS identity, its network rules) — a Prometeo
    AppService. Replaces tf-common-modules/modules/bootstrap-service. The product must already
    exist (run the "product" template first).
  tags:
    - prometeo
spec:
  owner: guests
  type: service
  parameters:
    - title: Placement
      required:
        - name
        - product
        - image
      properties:
        name:
          title: Service name
          type: string
          description: >-
            e.g. "add-fe". Becomes the XR name and its own namespace (one namespace per service,
            not per product).
          pattern: '^[a-z][a-z0-9-]*$'
          ui:autofocus: true
        product:
          title: Product
          type: string
          description: The product this service belongs to (must already exist in prometeo-products).
          ui:field: EntityPicker
          ui:options:
            catalogFilter:
              kind: System
            defaultKind: System
        image:
          title: Container image
          type: string
          description: Full image ref including tag or digest.
        containerPort:
          title: Container port
          type: integer
          default: 8080
          description: Backends typically use 8000/8001, frontends 3000.
        replicas:
          title: Replicas
          type: integer
    - title: Runtime
      properties:
        env:
          title: Environment variables
          type: object
          additionalProperties:
            type: string
          default: {}
        connections:
          title: Datastore connections
          type: array
          description: >-
            Names of datastore components in this same namespace to mount (their
            <name>-connection ConfigMap), e.g. ["db"].
          items:
            type: string
          ui:field: MultiEntityPicker
          ui:options:
            catalogFilter:
              kind: Resource
              spec.type: database
              spec.system: ${{ parameters.product }}
            defaultKind: Resource
          default: []
        migrationsEnabled:
          title: Run schema migrations before rollout
          type: boolean
          default: false
    - title: AWS access
      description: Opens the matching VPC interface endpoint's NetworkPolicy.
      properties:
        awsBedrock:
          title: Bedrock
          type: boolean
          default: false
        awsS3:
          title: S3
          type: boolean
          default: false
        awsSqs:
          title: SQS
          type: boolean
          default: false
        awsDynamodb:
          title: DynamoDB
          type: boolean
          default: false
    - title: Ingress
      properties:
        ingressEnabled:
          title: Expose through the shared ALB
          type: boolean
          default: false
        ingressHost:
          title: Host
          type: string
          description: Defaults to <product>.<environment domain> when left blank.
        ingressDnsRecord:
          title: Point DNS at the ALB
          type: boolean
          default: true
          description: Set false when an SsoEndpoint fronts this service instead.
        healthcheckPath:
          title: Health check path
          type: string
          default: /health
    - title: Network
      description: Everything is denied by default; each entry opens one direction, to one peer, on one port.
      properties:
        allowFrom:
          title: Allow from
          type: array
          items:
            type: object
            required: [service, port]
            properties:
              service:
                type: string
                ui:field: EntityPicker
                ui:options:
                  catalogFilter:
                    kind: Component
                    spec.system: ${{ parameters.product }}
                  defaultKind: Component
              port:
                type: integer
          default: []
        allowTo:
          title: Allow to
          type: array
          items:
            type: object
            required: [service, port]
            properties:
              service:
                type: string
                ui:field: EntityPicker
                ui:options:
                  catalogFilter:
                    kind: Component
                    spec.system: ${{ parameters.product }}
                  defaultKind: Component
              port:
                type: integer
          default: []

  steps:
    - id: write-component
      name: Write the AppService manifest
      action: roadiehq:utils:fs:write
      input:
        path: products/${{ parameters.product }}/base/${{ parameters.name }}.yaml
        content: |
          apiVersion: v1
          kind: Namespace
          metadata:
            name: ${{ parameters.name }}
            labels:
              name: ${{ parameters.name }}
              service: ${{ parameters.name }}
              product: ${{ parameters.product }}
              platform: prometeo
              admission.datadoghq.com/enabled: "true"
          ---
          apiVersion: platform.prometeo.iag.ai/v1alpha1
          kind: AppService
          metadata:
            name: ${{ parameters.name }}
            namespace: ${{ parameters.name }}
          spec:
            product: ${{ parameters.product }}
            image: ${{ parameters.image | dump }}
            containerPort: ${{ parameters.containerPort }}
          {% if parameters.replicas %}
            replicas: {{ parameters.replicas }}
          {% endif %}
          {% if parameters.env and parameters.env | length %}
            env:
          {% for k, v in parameters.env %}
              {{ k }}: {{ v | dump }}
          {% endfor %}
          {% endif %}
          {% if parameters.connections and parameters.connections | length %}
            connections:
          {% for c in parameters.connections %}
              - {{ c }}
          {% endfor %}
          {% endif %}
            migrations:
              enabled: ${{ parameters.migrationsEnabled }}
            aws:
              bedrock: ${{ parameters.awsBedrock }}
              s3: ${{ parameters.awsS3 }}
              sqs: ${{ parameters.awsSqs }}
              dynamodb: ${{ parameters.awsDynamodb }}
            ingress:
              enabled: ${{ parameters.ingressEnabled }}
          {% if parameters.ingressHost %}
              host: {{ parameters.ingressHost | dump }}
          {% endif %}
              dnsRecord: ${{ parameters.ingressDnsRecord }}
              healthcheckPath: ${{ parameters.healthcheckPath | dump }}
          {% if (parameters.allowFrom and parameters.allowFrom | length) or (parameters.allowTo and parameters.allowTo | length) %}
            network:
          {% if parameters.allowFrom and parameters.allowFrom | length %}
              allowFrom:
          {% for r in parameters.allowFrom %}
                - service: {{ r.service }}
                  port: {{ r.port }}
          {% endfor %}
          {% endif %}
          {% if parameters.allowTo and parameters.allowTo | length %}
              allowTo:
          {% for r in parameters.allowTo %}
                - service: {{ r.service }}
                  port: {{ r.port }}
          {% endfor %}
          {% endif %}
          {% endif %}

    - id: write-catalog-info
      name: Write the Backstage Component entity
      action: roadiehq:utils:fs:write
      input:
        path: products/${{ parameters.product }}/components/${{ parameters.name }}.yaml
        content: |
          apiVersion: backstage.io/v1alpha1
          kind: Component
          metadata:
            name: ${{ parameters.name }}
            description: ${{ ('AppService ' + parameters.name + ' (product ' + parameters.product + ')') | dump }}
            annotations:
              backstage.io/kubernetes-label-selector: 'service=${{ parameters.name }}'
          spec:
            type: service
            lifecycle: experimental
            owner: guests
            system: ${{ parameters.product }}

    - id: publish
      name: Open a PR with the new service
      action: publish:github:pull-request
      input:
        repoUrl: github.com?repo=prometeo-products&owner=crossplane-poc
        branchName: scaffolder/service-${{ parameters.name }}
        title: 'prometeo service: ${{ parameters.name }}'
        description: >-
          Adds `products/${{ parameters.product }}/base/${{ parameters.name }}.yaml` (Namespace +
          AppService) and its Backstage Component entity, via the "service" template.

  output:
    links:
      - title: Open PR
        url: ${{ steps['publish'].output.remoteUrl }}
